#!/bin/sh
# Print the default sink volume as 0-100, or "muted".
out=$(wpctl get-volume @DEFAULT_AUDIO_SINK@ 2>/dev/null) || { echo 0; exit 0; }
case "$out" in
*MUTED*) echo muted ;;
*) echo "$out" | awk '{printf "%d\n", $2*100}' ;;
esac
